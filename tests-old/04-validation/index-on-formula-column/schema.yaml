tables:
  products:
    columns:
      id:
        definition: serial primary key
      price:
        definition: numeric(10,2)
      quantity:
        definition: integer
      total:
        definition: numeric(10,2)
        formula: "@price * @quantity"
    # Index on generated column (valid)
    indexes:
      - [total]
