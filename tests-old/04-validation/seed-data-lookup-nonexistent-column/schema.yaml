tables:
  categories:
    columns:
      id:
        definition: serial primary key
      name:
        definition: varchar(100)

  products:
    columns:
      id:
        definition: serial primary key
      name:
        definition: varchar(100)
    foreign-keys:
      category_id: categories
    seed-rows:
      - id: 1
        name: "Widget"
        # $lookup where clause references non-existent column
        category_id_id:
          $lookup:
            table: categories
            where: {nonexistent_column: "Electronics"}
