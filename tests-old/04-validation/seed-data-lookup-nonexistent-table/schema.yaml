tables:
  categories:
    columns:
      id:
        definition: serial primary key
      name:
        definition: varchar(100)

  products:
    columns:
      id:
        definition: serial primary key
      name:
        definition: varchar(100)
    foreign-keys:
      category_id: categories
    seed-rows:
      - id: 1
        name: "Widget"
        # $lookup to non-existent table
        category_id_id:
          $lookup:
            table: nonexistent_table
            where: {name: "Electronics"}
