columns:
  id: serial primary key
  created_at: timestamp

tables:
  users:
    columns:
      id:           # Empty form - inherit from columns
      name: varchar(100)
      created_at:   # Empty form - inherit from columns
