tables:
  accounts:
    columns:
      id: integer primary key
      balance: numeric(12,2)
      interest_rate: real

  transactions:
    columns:
      transaction_id: serial primary key
      amount: decimal
      fee: double precision
