tables:
  projects:
    columns:
      id:
        type: serial primary key
      name:
        type: varchar(100)

  tasks:
    columns:
      id:
        type: serial primary key
      task_name:
        type: varchar(100)
    foreign_keys:
      project_id:
        table: projects
        auto_create:
          on: [insert]
          copy_columns:
            # Non-existent parent column
            nonexistent_parent_col: task_name
