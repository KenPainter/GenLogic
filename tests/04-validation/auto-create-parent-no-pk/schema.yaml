tables:
  # Parent table WITHOUT a primary key
  categories:
    columns:
      category_name:
        definition: varchar(100)
      total_amount:
        definition: integer default 0

  # Child table attempting auto_create_parent on FK to table without PK
  transactions:
    columns:
      transaction_id:
        definition: serial primary key
      amount:
        definition: integer
    foreign_keys:
      category_name:
        table: categories
        auto_create_parent: true
