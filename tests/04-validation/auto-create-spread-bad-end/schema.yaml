tables:
  projects:
    columns:
      id:
        definition: serial primary key
      start_date:
        definition: date

  tasks:
    columns:
      id:
        definition: serial primary key
      date:
        definition: date
    foreign_keys:
      project_id:
        table: projects
        auto_create:
          on: [insert]
          spread:
            start: start_date
            # Non-existent end column
            end: nonexistent_end
            interval: day_interval
            generated_column: date
