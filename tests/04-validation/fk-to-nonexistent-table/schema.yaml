tables:
  orders:
    columns:
      id:
        type: serial primary key
      product_name:
        type: varchar(100)
    foreign_keys:
      # References a table that doesn't exist
      customer_id: customers
