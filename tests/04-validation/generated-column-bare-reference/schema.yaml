tables:
  products:
    columns:
      id:
        type: serial primary key
      price:
        type: numeric(10,2)
      quantity:
        type: integer
      # Bare column reference without @ (should use @ sigil)
      total:
        type: numeric(10,2)
        generated: "price * @quantity"
