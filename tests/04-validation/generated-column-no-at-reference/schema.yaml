tables:
  products:
    columns:
      id:
        type: serial primary key
      price:
        type: numeric(10,2)
      quantity:
        type: integer
      # Generated column with no @ references (invalid)
      total:
        type: numeric(10,2)
        generated: price * quantity
