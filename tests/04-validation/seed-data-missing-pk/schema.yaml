tables:
  products:
    columns:
      # Non-serial PK requires explicit value
      code:
        definition: varchar(20) primary key
      name:
        definition: varchar(100)
    seed-rows:
      # Missing required PK column
      - name: "Widget"
