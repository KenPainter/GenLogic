tables:
  categories:
    columns:
      id:
        definition: serial primary key
      name:
        definition: varchar(100)

  products:
    columns:
      id:
        definition: serial primary key
      name:
        definition: varchar(100)
    foreign_keys:
      # Nullable FK (not_null: false)
      category_id:
        table: categories
        not_null: false
    # Unique constraint on nullable FK (valid but may warn)
    unique_constraints:
      - [category_id]
