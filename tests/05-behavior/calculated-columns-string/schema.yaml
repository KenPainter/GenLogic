tables:
  contacts:
    columns:
      id: integer primary key
      first_name: varchar(50)
      last_name: varchar(50)
      full_name:
        type: varchar(101)
        generated: "first_name || ' ' || last_name"
