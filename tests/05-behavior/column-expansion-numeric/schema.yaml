# Test NUMERIC column precision/scale expansion
# Initial: NUMERIC(10,2)
# Expanded: NUMERIC(12,4)
tables:
  products:
    columns:
      product_id: serial primary key
      product_name: varchar(50)
      price: numeric(12,4)
      quantity: numeric(10,2)
