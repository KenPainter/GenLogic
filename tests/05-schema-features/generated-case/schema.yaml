tables:
  orders:
    columns:
      id: serial primary key
      status: varchar(20)
      is_pending:
        definition: boolean
        generated: "CASE WHEN @status = 'pending' THEN TRUE ELSE FALSE END"
