tables:
  users:
    columns:
      id:
        type: serial primary key
        label: User ID
        format: id

  posts:
    columns:
      id: serial primary key
    foreign_keys:
      user: users  # Should inherit label/format
