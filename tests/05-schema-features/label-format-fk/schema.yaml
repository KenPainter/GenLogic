tables:
  users:
    columns:
      id:
        definition: serial primary key
        label: User ID
        format: id

  posts:
    columns:
      id: serial primary key
    foreign-keys:
      user: users  # Should inherit label/format
