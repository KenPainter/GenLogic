columns:
  email_col:
    definition: varchar(255)
    label: Email Address
    format: email

tables:
  test:
    columns:
      id: serial primary key
      email: email_col  # Inherit label/format
