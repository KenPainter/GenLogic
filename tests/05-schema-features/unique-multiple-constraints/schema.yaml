tables:
  test:
    columns:
      id: serial primary key
      email: varchar(255)
      username: varchar(50)
      ssn: varchar(11)
    unique_constraints:
      - [email]
      - [username]
      - [ssn]
