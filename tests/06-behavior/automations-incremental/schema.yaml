tables:
  accounts:
    columns:
      id: integer primary key
      balance:
        type: numeric(10,2)
        automation: SUM @transactions.amount

  transactions:
    columns:
      id: integer primary key
      account_id: integer
      amount: numeric(10,2)
    foreign_keys:
      account: { table: accounts }
