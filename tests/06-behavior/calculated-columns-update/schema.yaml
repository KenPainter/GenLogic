tables:
  products:
    columns:
      id: integer primary key
      base_price: numeric(10,2)
      markup: numeric(5,2)
      final_price:
        definition: numeric(10,2)
        formula: "@base_price * (1 + @markup / 100)"
