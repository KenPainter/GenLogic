tables:
  orders:
    columns:
      order_id: serial primary key
      price: numeric(10,2)
      quantity: integer
      total:
        definition: numeric(10,2)
        formula: "@price * @quantity"
