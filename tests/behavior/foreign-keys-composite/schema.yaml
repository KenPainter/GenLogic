tables:
  order_headers:
    columns:
      order_id: integer primary key
      order_year: integer primary key
      customer_name: varchar(100)

  order_lines:
    foreign_keys:
      order: { table: order_headers }
    columns:
      line_id: serial primary key
      product: varchar(100)
      quantity: integer
