tables:
  users:
    columns:
      id: integer primary key
      name: varchar(50)

  posts:
    columns:
      id: integer primary key
      title: varchar(100)
      user_id: integer
    foreign_keys:
      user: { table: users }
