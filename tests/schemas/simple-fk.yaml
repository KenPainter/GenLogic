# Simple foreign key - child references parent
tables:
  accounts:
    columns:
      account_id: serial primary key
      name: varchar(100)

  transactions:
    columns:
      id: serial primary key
      account_id: FK accounts
      amount: numeric(10,2)
